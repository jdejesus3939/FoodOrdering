<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.jproject.opensource.foodordering"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="10" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="info.jproject.opensource.foodordering.DashboardActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".HallwayActivity" />
        <activity android:name=".InfoActivity" />
        <!-- <activity android:name=".EinsteinBrosInfoActivity" />  -->
        <activity android:name=".EinsteinBrosMainActivity" />
        <!-- <activity android:name=".SimplyToGoInfoActivity" />  -->
        <activity android:name=".SimplyToGoMainActivity" />
        <!-- <activity android:name=".StarbucksInfoActivity" />  -->
        <activity android:name=".StarbucksMainActivity" />
        <!-- <activity android:name=".SubwayInfoActivity" />  -->
        <activity android:name=".SubwayMainActivity" />
        <!-- <activity android:name=".UltimateBajaInfoActivity" />  -->
        <activity android:name=".UltimateBajaMainActivity" />
        <!-- <activity android:name=".UnivCenterInfoActivity" />  -->
        <activity android:name=".UnivCenterMainActivity" />
        <activity android:name=".CheckoutActivity" />
        <activity android:name=".ThankYouActivity" />
        
    </application>

</manifest>
